{
  "name": "node-test",
  "version": "0.0.1",
  "main": "dist/index.js",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "production": "babel src --out-dir dist --copy-files --include-dotfiles",
    "development": "babel src --out-dir dist --copy-files --include-dotfiles",
    "watch": "babel src --out-dir dist --copy-files --include-dotfiles --watch --verbose"
  },
  "devDependencies": {
    "@babel/cli": "^7.6.0",
    "@babel/core": "^7.6.0",
    "@babel/preset-env": "^7.9.5"
  }
}
